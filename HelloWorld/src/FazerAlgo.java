/* Instruções: declarações, atribuições, chamadas de métodos, etc..

    int x = 3;
    String name = "Dirk";
    x = x * 17;
    System.out.print("X is " + x);
    double d = Math.random();
    // isto é um comentário

/* Fazer algo repetidamente 
 * Loops: for e while



while (x > 12) {
    x = x -1;
}

for (int x = 0; x < 10; x = x + 1) {
    System.out.print("x is now " + x);
}


/* Fazer algo sob essa condição
 * Ramificação: testes if/else


 if (x == 10) {
    System.out.print("x must be 10");
 } else {
    System.out.print("x isn't 10");
 }
 if ((x < 3)) & (name.equals("Dirk")) {
    System.out.println("Gently")
 }
 System.out.print("this line runs no matter what");


 int x = 4; // atribui 4 a x
 while (x > 3) {
    // o código do loop será executado porque
    // x é maior que 3
    x = x - 1; // ou ficaríamos eternamente no loop
 }
 int z = 27; //
 while (z == 17) {
    // o código do loop não será executado porque
    // z não é igual a 17
 }

 */